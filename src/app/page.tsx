import React from 'react'

export default function Home() {
  // The MainLayout in layout.tsx handles content rendering via navigation state
  // Return empty fragment so MainLayout's renderSection() is called instead of showing children
  return <></>
}