import React from 'react'

export default function Home() {
  // This page is now handled by the MainLayout component
  // which renders different sections based on navigation state
  return (
    <div>
      {/* The MainLayout in layout.tsx will render the appropriate content */}
    </div>
  )
}